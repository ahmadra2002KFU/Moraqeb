services:
  moraqeb:
    build: .
    container_name: moraqeb
    ports:
      - "${PORT:-3117}:${PORT:-3117}"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./runs:/app/runs
    restart: unless-stopped
